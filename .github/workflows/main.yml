name: Main CI

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  flutter-analyze:
    name: Flutter analyze
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/flutter_app
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.16.9"
          channel: stable
      - run: flutter pub get
      - run: dart analyze

  flutter-test:
    name: Flutter test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/flutter_app
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.16.9"
          channel: stable
      - run: flutter pub get
      - run: flutter test --reporter expanded

  python-lint:
    name: Python lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install black==24.1.0 ruff==0.1.14 mypy==1.8.0
      - run: black --check --diff app/ tests/
      - run: ruff check app/ tests/
      - run: mypy app/

  pytest:
    name: Python tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pytest==7.4.4 pytest-asyncio==0.23.3 pydantic==2.5.0
      - run: pytest

  build-apk:
    name: Build APK
    needs: [flutter-analyze, flutter-test, python-lint, pytest]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/flutter_app
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.16.9"
          channel: stable
      - run: flutter pub get
      - run: flutter create --platforms=android .
      - run: flutter build apk --debug
      - uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: src/flutter_app/build/app/outputs/flutter-apk/app-debug.apk
